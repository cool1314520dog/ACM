#include<cstdio>
#include<cstring>
int main()
{
    int n,i,flag[10];
    while(scanf("%d",&n)!=EOF)
    {:wq

        for(i=1234;i<5000;i++)
        {
            memset(flag,0,sizeof(flag));
            while(s1||s2)
            {
                if(!flag[s1])

